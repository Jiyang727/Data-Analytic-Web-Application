// Generated by CoffeeScript 1.12.2
(function() {
  var signature;

  signature = require('./signature-checker');

  module.exports = function(args, hasData) {
    var opts;
    if (hasData == null) {
      hasData = false;
    }
    opts = {};
    if (signature.matches(args, 'string')) {
      opts.config = {
        url: args[0]
      };
      if (hasData) {
        opts.config.json = {};
      } else {
        opts.config.json = true;
      }
    } else if (signature.matches(args, 'object')) {
      opts.config = args[0];
      if (opts.config.json === void 0) {
        opts.config.json = true;
      }
    } else if (signature.matches(args, 'string', 'object') && hasData) {
      opts.config = {
        url: args[0],
        json: args[1]
      };
    } else {
      throw new Error("Invalid params: " + (args.join(', ')));
    }
    return opts;
  };

}).call(this);
