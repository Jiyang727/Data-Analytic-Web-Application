// Generated by CoffeeScript 1.12.2
(function() {
  var getOpts;

  getOpts = require('./get-options');

  module.exports = function(method, args) {
    var opts;
    opts = {};
    method = method.toUpperCase();
    switch (method) {
      case 'GET':
      case 'DELETE':
      case 'HEAD':
        opts = getOpts(args, false);
        break;
      case 'POST':
      case 'PUT':
      case 'PATCH':
        opts = getOpts(args, true);
        break;
      default:
        throw new Error('Method must be GET, DELETE, POST, PUT, PATCH or HEAD');
    }
    opts.config.method = method;
    return opts;
  };

}).call(this);
